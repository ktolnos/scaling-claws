I want to rework how space era works. Here is rough scatch of what I have in mind:
- Rearrange the resources such that amounts and income can be displayed for Earth Moon Mercury (table columns). Add labor to the displayed resources, labor should also be separate for each place. On the Moon and the Mercury the labor will be generated by robots. 
Columns unlock as player unlocks corresponding tehnologies (feel free to modify the tech tree, it is in early stage of development). 

Each facility also should have a separate number of buildings with a separate buy buttons for Earth, Moon, and Mercury. You may arrange them in the same columns. The column titles should only appear when the tech is unlocked and before Moon is unlocked by science, the Earth title should be hidden. Be mindful of the screen space, the panel itself is pretty narrow (300px) and should fit on the screen vertically.

Each building on each planet/moon should have a separate tech that unlocks it on Earth and on the Moob, except for mines on earth. Before unlocking, the building shouldn't be visible. Mass driver is not available on Earth and there is no corresponding tech.

Rework ENERGY & SPACE panel. It is currently poorly written. Refactor it to use the same code to render the earth energy as before transition from ENERGY to ENERGY & SPACE. Remove any duplicating code. Feel free to refactor in any way you like, including changing from 2 separate panels into 1.

The contents of the space part will be different. Since buying factories moved to the supply chain panel, this panel will be focused only on energy and space logistics.

Add a bulk buy button for installing solar panels after their manufacturing is unlocked. The solar tech should unlock the manufacturing the installation. The amount of the solar panels installable on Earth should be limited by some reasonable number. Installing them should cost labor and solar panels and increase power generation. Change the powerplants and datacenters to also use the bulk buy buttons (which are already implemented). Limit the number of datacenters of each type. Remove old code regarding the solar panels on earth if it ends up to be unused.

The space logistics is all about moving the payloads between Earth, Moon, and Mercury using rockets and mass drivers. Mass drivers still consume rockets, but they massively increase the capacity. They have limited launches/min, so building more of them allows to send more rockets with increased capacity. Rocket capacity also depends on where you launch it from, from the earth or from the moon. User can order how much of what should be moved to where and the rockets will execute these orders over time. Rockets are consumed, unless reusable rockets are researched. Add multiple tiers of rockets reusability, which should decrease the percentage of the rockets lost.

Things that need to be sent:
Earth -> Orbit 
- GPU sattelites
Earth -> Moon
- GPUs
- Solar
- Robots
Moon -> Mercury
- Robots
Each resource should have bulk buttons to schedule sending the payload. This consumes the resources immediately at the source. There should be a text to show how much is already sent. Maybe add a thin progress bar on the bottom of each resource if space permits. Rockets take some time to actually deliver the payload, after the delivery resource is added at destination. Reusable rockets need time to get back. Rockets are allocated equally across all destinations/resources. Avoid tracking individual rockets, the game should operate with quadrillions of rockets smoothly (so only O(1) operations). Use biginteger for all resources, utils.ts contains some utilities for fixed point computations.

Alteration to how robots work: instead of a separate job (Robot Worker), robots generate labor wherever they are (e.g. robots on earth generate labor on Earth). This should be explained somewhere in the UI (or at least in the TopBar breakdown and the tech description). First generation robots produce little labor, with more research making them more productive.

Here is a rough progression path:
First, the player can choose in any order
- Solar panel manufacturing. 
- Chip manufacturing
- Robots
All of these require materials and labor to manufacture. All factories also cost materials and labor, mines cost only labor.

Next:
- GPU sattelites production
- Rocketry

GPU sattelites are made from GPUs and solar panels.
Rockets are made from materials and labor.
GPU sattelite or rocket tech also unlock UI for launching GPU sattelites into orbit. The number of sattelites in orbit should be displayed in the ENERGY & SPACE panel.

Next: 
- A tech for sending payload to the moon. Unlocks UI for sending Robots, GPUs and Solar to the moon and UI for installing solar panels and GPUs on the moon (no datacenters needed, although there should be some big limit, e.g. 1T). Installation consumes labor.
- Then moon building techs: mines, solar manufacturing, GPU manufacturing, solar sattelites manufacturing, rocket manufacturing, mass dirvers. Each unlocks buttons to buy these buildings on the moon in the supply panel.
- A tech for sending payload to the Mercury. Unlocks UI to send robots from the Moon to Mercury. Mercury doesn't support GPU installation, it's sole purpose is to be mined out for Dyson swarm. This tech also unlocks all buildings on Mercury. The difference for the Mercury is that the number of all buildings is unlimited. The percentage of the Mercury's mass left should be displayed as a progress bar.
- And the last tech to develop von Neumann probes, which add a button for sending these to the UI. The button triggers the end of the game.











